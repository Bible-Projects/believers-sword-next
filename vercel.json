{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "cd FrontEndApp && yarn install --frozen-lockfile",
    "buildCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then echo \"Skipping build for non-main branch\"; exit 0; else cd FrontEndApp && yarn build:web; fi",
    "outputDirectory": "dist-web",
    "framework": null,
    "rewrites": [
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}
